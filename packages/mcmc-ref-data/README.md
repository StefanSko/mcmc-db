# mcmc-ref-data

Reference draws package for `mcmc-ref`.

This package is intended to ship pre-built `draws/` and `meta/` artifacts so
consumers can run comparisons immediately after installing `mcmc-ref[data]`.
